{% extends '@bolt/_partials/fields/_base.html.twig' %}

{% block field %}
    <input type="color" class="form-control color-field" name="{{ name }}" id="{{ id }}" value="{{ value }}"/>
{% endblock %}
